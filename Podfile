platform :ios, '5.0'

# hide warnings produced by Pods
# remove this and run 'pod install' if there are problems with dependencies
# that you're trying to debug
inhibit_all_warnings!

pod 'CocoaAsyncSocket'